@model IEnumerable<BeautySalonBooking.Models.Booking>
@{
    ViewData["Title"] = "Bookings";
}

<h2>Bookings</h2>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Service</th>
            <th>Customer</th>
            <th>Date</th>
            <th>Comment</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model)
{
    <tr>
        <td>@item.Service?.ServiceName</td>
        <td>@item.FirstName @item.LastName</td>
        <td>@item.Date.ToString("yyyy-MM-dd HH:mm")</td>
         <td>@item.Comment</td>
        <td>
            <a class="btn-edit btn-sm" asp-controller="Booking" asp-action="Edit" asp-route-id="@item.Id">Edit</a>
            <a class="btn-delete btn-sm" asp-controller="Booking" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
        </td>
    </tr>
}
    </tbody>
</table>
